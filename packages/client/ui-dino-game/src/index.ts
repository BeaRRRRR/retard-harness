/**
 * Dino-runner distraction plugin, node half.
 *
 * Deliberately empty. The dino game is a browser-only pastime that renders
 * while a session is running; there is no host-side surface it needs to own.
 */

/** Host plugin body — the dino game lives entirely in the browser half. */
export function apply(): void {}
